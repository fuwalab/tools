languages: go

go:
  - "1.10.x"

before_install:
  #- curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
  - mkdir $GOPATH/src/github.com
  - mv $HOME/build/ $GOPATH/src/github.com/

install:
  #- $GOPATH/bin/dep ensure -update
  - go get github.com/atotto/clipboard/...
  - go get -t ./...

script:
  - go test -v ./...

notifications:
  email: false
