languages: go

go:
  - "1.10.x"

before_install:

install:
  - go get -t ./...
  - rm -rf $GOPATH/src/github.com/fuwalab
  - cp -r $HOME/build/fuwalab $GOPATH/src/github.com/

script:
  - go test -v ./...

notifications:
  email: false
